import { Button } from "@/components/ui/button.tsx";

export default function App() {
    return (
        <>
            <div>Music Resource Platform</div>
            <Button>Đăng ký</Button>
            <Button>Đăng nhập</Button>
        </>
    );
}
